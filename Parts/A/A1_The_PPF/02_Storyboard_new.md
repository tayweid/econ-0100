# Episode A1 | Production Possibility Frontier | Storyboard

## 0.a · Wordmark

- Fade in the centered raster `MICROECONOMICS` wordmark.
- Continue directly into 0.b without a hold.

## 0.b · Flicker

- Flicker the raster through the blue palette.
- Continue directly into 0.c without a hold.

## 0.c · Part and thesis

1. Move the raster up and add `Part A | Episode 1` beneath it.
2. Flicker the raster again after the part label arrives.
3. Fade in *The space of what's possible* beneath the part label.
4. Keep the completed bumper as one vertically centered block.

## 0.d · Last time

1. Clear the bumper.
2. Fade in a full-frame `Last Time...` card.

## 1.a · Choice recap

1. Fade out `Last Time...`.
2. Fade in the title `Opportunity Cost` at the upper left.
3. Build `A or B` one item at a time.
4. Fade in equal-sized choice boxes: green around chosen `A`, red around
   given-up `B`.

## 1.b · First opportunity cost

1. Write `Opportunity Cost(A) = B` beneath `A or B`.
2. Color `A` green and `B` red to match the boxes.

## 1.c · Reverse the choice

1. Swap the green and red boxes so `B` is chosen and `A` is given up.
2. Transform the equation into `Opportunity Cost(B) = A`.
3. Swap the letter colors with the choice.

## 1.d · Definition

1. Keep the recap stage in place.
2. Write `Opportunity Cost is the value of the next best use of your
   resources.` along the bottom of the frame, with the defined term in gold.

## 1.e · Toy-model camera stretch

1. Cut to full-frame camera for the pool-table and toy-model discussion.
2. Run no animation during this passage.
3. Return to animation at 2.a.

## 2.a · Molly's farm

1. Clear the recap stage.
2. Fade in Molly's farm outline and the label `Molly's Farm` on the left.

## 2.b · All spinach

1. Fill the entire farm spinach-green.
2. Slide `40 S` from the farm to the right side of the frame.

## 2.c · All carrots

1. Morph the farm from all spinach to all carrots.
2. Slide in `10 C` and `or` so the right side reads `10 C or 40 S`.

## 2.d · Set the extremes equal

1. Transform `or` into `=`.
2. Push the camera in on `10 C = 40 S`.

## 2.e · Divide through

- Transform the equation into `10/10 C = 40/10 S` while holding the layout
  fixed.

## 2.f · Simplify

1. Reduce the equation to `1 C = 4 S`.
2. Fade out the farm outline, farm name, and crop fills while keeping the
   equation.

## 2.g · Name the opportunity cost

1. Reframe the camera to keep the reduced equation and result in view.
2. Write `Opportunity Cost(1 C) = 4 S` above `1 C = 4 S`.

## 3.a · Axes and first endpoint

1. Clear the opportunity-cost stage and reset the camera to the full frame.
2. Rebuild Molly's farm on the left, filled entirely with carrots.
3. Grow axes on the right with carrots horizontal and spinach vertical; show
   the relevant carrot ticks through 10 and spinach ticks through 40 in their
   respective colors.
4. Place a guide-red dot at `(10, 0)`.
5. Add a live `(C, S)` readout beside the dot, with the carrot value orange and
   the spinach value green.

## 3.b · Second endpoint

1. Leave a fixed mark at `(10, 0)`.
2. Morph the farm from all carrots to all spinach in one continuous move.
3. Ride the live dot and readout to `(0, 40)`.
4. Leave a fixed mark at `(0, 40)`.

## 3.c · Half split

1. Split the farm half carrots and half spinach.
2. Ride the dot to `(5, 20)` and leave a fixed mark.

## 3.d · Three-fifths carrots

1. Split the farm three-fifths carrots and two-fifths spinach.
2. Ride the dot to `(6, 16)` and leave a fixed mark.

## 3.e · Three-tenths carrots

1. Split the farm three-tenths carrots and seven-tenths spinach.
2. Ride the dot to `(3, 28)` and leave a fixed mark.

## 3.f · Draw the PPF

1. Draw Molly's PPF through the accumulated marks, from `(0, 40)` to
   `(10, 0)`.
2. Transform `Molly's Farm` into the upper-left title
   `Molly's Production Possibility Frontier`.
3. Fill the farm Molly-blue.
4. Fade out the separate carrot and spinach fills, moving dot, live readout,
   and point marks.

## 3.g · PPF equation

1. Write `S = 40 - 4C` on the graph.
2. Color `S` spinach-green and `C` carrot-orange.

## 3.h · Slope triangle

1. Draw a carrot-orange horizontal segment from `(4, 24)` to `(5, 24)` and
   label it `+1 C`.
2. Draw a spinach-green vertical segment from `(5, 24)` to `(5, 20)` and label
   it `-4 S`.
3. Fade in a gold box around the `-4` coefficient in the PPF equation.

## 3.i · Connect slope and opportunity cost

- Write `Opportunity Cost(1 C) = 4 S` beneath the PPF equation, aligned to its
  left edge.

## 3.j · Exercise A1 Q1

1. Fade the existing stage to five percent opacity.
2. Fade in an opaque rounded exercise panel.
3. Write the gold heading `Exercise A1 | Q1`.
4. Use `Q1 | Hagrid's PPF` after the `#beat-3.j` link as the question subhead.
5. Present the following italic question paragraph as one wrapped paragraph.

## 4.a · Rebuild for attainability

1. Clear the exercise and prior stage.
2. Fade in the title `Attainability`.
3. Rebuild Molly's farm on the left, shifted down enough to clear the title,
   with a solid Molly-blue fill.
4. Build a larger graph on the right covering the original intercepts
   `(0, 40)` and `(10, 0)` and the later carrot intercept `(16, 0)`; keep
   carrots horizontal and spinach vertical.
5. Fade in the original PPF in Molly-blue with a coincident muted ghost behind
   it.

## 4.b · Inefficient point

1. Shade the attainable triangle bounded by the axes and original PPF in
   Molly-blue.
2. Hold on the shaded region.
3. Fade in a muted dot at `(4, 16)` with the label `Inefficient` below and
   slightly left.

## 4.c · Efficient point

- Fade in a green dot at `(6, 16)` with the label `Efficient` above and right.

## 4.d · Unattainable point

1. Fade in a guide-red dot at `(8, 28)`.
2. Hold on the unlabeled point.
3. Fade in `Unattainable` above it.

## 4.e · Exercise A1 Q2

1. Fade the attainability stage to five percent opacity.
2. Fade in an opaque rounded exercise panel.
3. Write the gold heading `Exercise A1 | Q2`.
4. Use `Q2 | Feasibility` after the `#beat-4.e` link as the question subhead.
5. Present the following italic question paragraph as one wrapped paragraph.

## 5.a · Reduce labor

1. Fade out the Q2 panel and restore the attainability stage to full opacity.
2. Fade out the attainable-region fill and all three classification dots and
   labels.
3. Fade in guide-red endpoint dots at the original intercepts `(10, 0)` and
   `(0, 40)`.
4. Narrow the planted farm band to four-fifths of its original width, aligned
   to the farm's left edge.
5. Add the vertical label `- Labor Cut` inside the narrowed band.
6. Slide the endpoint dots to `(8, 0)` and `(0, 32)` as the band narrows.

## 5.b · Shift the frontier inward

1. Transform the active PPF to the labor-cut frontier from `(0, 32)` to
   `(8, 0)`.
2. Keep both endpoint dots visible for the technology comparison.

## 5.c · Add carrot technology

1. Add the vertical label `+ Better Carrot Tech` beside `- Labor Cut` on the
   farm.
2. Hold on the question before changing the graph.

## 5.d · Pivot the frontier outward

1. Slide the carrot endpoint from `(8, 0)` to `(16, 0)` while the spinach
   endpoint remains at `(0, 32)`.
2. Leave the labor-cut frontier behind as a muted ghost.
3. Pivot the active PPF out to the technology frontier from `(0, 32)` to
   `(16, 0)`.
4. Fade out both endpoint dots.

## 5.e · No longer attainable

1. Fade in the lost region bounded by `(0, 32)`, `(0, 40)`, and the frontier
   intersection `(4, 24)` in the lost-region color.
2. Hold on the region.
3. Fade in `No longer attainable` to its right.

## 5.f · Newly attainable

1. Fade in the gained region bounded by `(10, 0)`, `(16, 0)`, and `(4, 24)` in
   the gained-region color.
2. Hold on the region.
3. Fade in `Newly attainable` to its right.

## 5.g · Lost and regained

1. Fade in the gold sliver bounded by `(10, 0)`, `(8, 0)`, `(0, 32)`, and
   `(4, 24)`—the area lost to the labor cut and regained by the technology.
2. Place a gold `?` at the sliver's center of mass.
3. Leave the question on screen.

## 5.h · Exercise A1 Q3

1. Fade the existing stage to five percent opacity.
2. Fade in an opaque rounded exercise panel.
3. Write the gold heading `Exercise A1 | Q3`.
4. Use `Q3 | Dynamics` after the `#beat-5.h` link as the question subhead.
5. Present the following italic question paragraph as one wrapped paragraph.

## 6.a · Closing camera stretch

1. Cut from the exercise card to full-frame camera for the closing synthesis.
2. Run no animation during the closing passage.
3. Return to animation on `We can actually do better.`

## 6.b · Punchline

1. Clear the prior stage to black.
2. Write the gold full-frame statement `We can actually do better.`

## 6.c · Next time

1. Clear the statement.
2. Show the upper-left title `Next time...` and the line
   `A detour into 1800s economic history.`
3. Emphasize the topic with the framebox gesture.
4. Clear the frame to black without a trailing hold.
